# ============================================================
# ETHDenver 2026 Campaign - Docker Environment Variables
# ============================================================
# Copy to .env.docker and fill in your credentials:
#   cp .env.docker.example .env.docker
#
# Dashboard-only mode (just demo) needs NO env vars.
# Agent mode (just live) requires the vars below.
# ============================================================

# Dashboard host port (change if 3000 is already in use)
DASHBOARD_PORT=3000
# CRE bridge host port (change if 8080 is already in use)
CRE_BRIDGE_PORT=8080
# Dashboard data mode (true=mock UI feed, false=live HCS feed)
NEXT_PUBLIC_USE_MOCK=true

# --- Shared Hedera Testnet Topics ---
# All three agents must use the SAME two HCS topics.
# The coordinator and worker agents use different env var names
# but they must resolve to the same Hedera topic IDs.
#
# Task topic (coordinator publishes assignments, workers subscribe):
HCS_TASK_TOPIC_ID=
HCS_TASK_TOPIC=
DEFI_HCS_TASK_TOPIC=
#
# Status/result topic (workers publish results, coordinator subscribes):
HCS_STATUS_TOPIC_ID=
HCS_RESULT_TOPIC=
DEFI_HCS_RESULT_TOPIC=
#
# HTS payment token for agent compensation:
HTS_PAYMENT_TOKEN_ID=

# --- Coordinator Agent ---
HEDERA_COORDINATOR_ACCOUNT_ID=
HEDERA_COORDINATOR_PRIVATE_KEY=
HEDERA_AGENT1_ACCOUNT_ID=
HEDERA_AGENT2_ACCOUNT_ID=
# CRE bridge endpoint for Chainlink demo mode (set automatically by `just chainlink up` if empty)
CRE_ENDPOINT=

# --- Inference Agent ---
INFERENCE_HEDERA_ACCOUNT_ID=
INFERENCE_HEDERA_PRIVATE_KEY=
INFERENCE_AGENT_ID=inference-001

# 0G Chain (shared key for all 0G integrations)
ZG_CHAIN_RPC=https://evmrpc-testnet.0g.ai
ZG_CHAIN_PRIVATE_KEY=

# 0G Compute
ZG_SERVING_CONTRACT=
ZG_COMPUTE_ENDPOINT=

# 0G Storage
ZG_FLOW_CONTRACT=0x22E03a6A89B950F1c82ec5e74F8eCa321a105296
ZG_STORAGE_NODE_ENDPOINT=
ZG_STORAGE_ENDPOINT=

# 0G iNFT (ERC-7857)
ZG_INFT_CONTRACT=
ZG_ENCRYPTION_KEY=
ZG_ENCRYPTION_KEY_ID=default

# 0G DA (audit trail)
ZG_DA_CONTRACT=0xE75A073dA5bb7b0eC622170Fd268f35E675a957B
ZG_DA_NAMESPACE=inference-audit
ZG_DA_ENDPOINT=

# --- DeFi Agent ---
DEFI_HEDERA_ACCOUNT_ID=
DEFI_HEDERA_PRIVATE_KEY=
DEFI_AGENT_ID=defi-001

# Base Sepolia
DEFI_BASE_RPC_URL=https://sepolia.base.org
DEFI_PRIVATE_KEY=
DEFI_WALLET_ADDRESS=

# Base contracts
DEFI_ERC8004_CONTRACT=
DEFI_DEX_ROUTER=
DEFI_BUILDER_CODE=
DEFI_ATTRIBUTION_ENABLED=true
DEFI_MOCK_MODE=true

# Trading pair (USDC/WETH on Base Sepolia)
DEFI_TOKEN_IN=0x036CbD53842c5426634e7929541eC2318f3dCF7e
DEFI_TOKEN_OUT=0x4200000000000000000000000000000000000006
